2026-09-13
Initialized the user-designated empty repository. Inherited Arc 1 directory, documentation and Git rules; added SRW-specific requirements.
Imported analysis scripts and recorded prior runtime test results. Parameterized runner/audit local paths through environment variables.
Re-ran imported audit successfully; Python compilation passed. Original and patched images unchanged.
2026-09-13: Added arc_comparison_report.md comparing historical Arc VRAM cache failures with the scoped SRW RAM font probe; no new runtime test or image mutation.

2026-09-13: Added SFC IPS identity survey; supplied patched ROM exactly reconstructs from supplied original and IPS. Viewed Korean character settings/name input in isolated Snes9x.
Built static PS1 B-font test image, CUE and xdelta in ignored output directory. Identified 1245 glyphs by exact bitmap matching, replaced both disk font copies, validated changed sectors and xdelta roundtrip.
Added actual A/B/A runtime probe and result report. Extended libretro runner to respect core need_fullpath for the SFC reference; PS1 A/B/A suite rerun passed after change.
User correction: withdrew Bold test build from use. Preserve existing precomposed appearance; SFC is a translation reference. Retain executed diagnostics as historical evidence only.
Added same-typeface six-slot provider probe, independent Unicode-to-screen oracle, 1800-glyph results and local quick-test launcher. No disc edits or translation changes. Removed dependency on withdrawn Bold-build mapping by reconstructing the six identities directly from baseline RAM.

2026-09-13 native loader and layout: implemented MAP-only six-source-glyph/one-entry RAM cache; existing Regular font and VRAM output preserved. 3552 legacy MIPS cases, 18 virtual cases and five emulator regressions passed. Cold boot and published-path cold boot (3600 frames, no external edits/state restore) passed pixel and cache-counter checks. Bulk font supply/battle/long-text work remains. BIOS SHA1 mismatch limits qualification. See native_loader_report.md.
Moved repository root to E:/4robot and adopted requested 00_original..99_backup layout. Inputs relocated with four PS1 hashes unchanged; old experiments retained under 01_work/experiments. Current BIN/CUE/xdelta published to 03_output/v001_native_loader. Added tracked directory READMEs; binaries remain ignored. UTF-8 config loading fixed after cp949 replay failure; published CUE replay passed with local relocated core. See layout_migration.json and native_published_path_verification.json.

2026-09-13 CUE portability fix: user emulator failed opening Track 2 with errno 22. Previous parent-directory audio references replaced by same-folder SRW4S_Track02.bin / SRW4S_Track03.bin copies with verified source hashes. Game BIN unchanged. Publisher skips identical existing files after WinError 32 on emulator-open BIN. Corrected CUE cold boot passed 3600 frames and exact glyph checks on libretro; user emulator retest pending.

2026-09-13 opening translation review: read-only PS1 paired extraction and SFC opening captures. Corrected loose input glob which initially selected patched BIN for both inputs; discarded 13745 interim candidates. Exact suffix matching and distinct SHA256 gate yield 14105 token-terminated paired slots (11907 original offsets; not dialogue count). Reviewed H02 E186-E226, including personality variants. No severe semantic omission established in this limited set; terminology/style editing remains worthwhile. SFC full script extraction not completed. See opening_translation_comparison.md and opening_comparison_sources.json.
Added opt-in capture_only video conversion to runner for low-cost screenshot sampling; 300-frame same-state SFC replay has identical captured PNGs and final RAM with option on/off. Game emulation still runs every frame. No ROM or translation edits.
